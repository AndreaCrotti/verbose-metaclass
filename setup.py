from setuptools import setup

package = 'verbose_metaclass'
version = '0.1'

setup(
    name=package,
    version=version,
    description="Give out more tracing information of a class execution",
    url='url'
)
